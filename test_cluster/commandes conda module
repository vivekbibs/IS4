module load conda
conda search -c conda-forge -c bioconda <tool>
module avail 2>&1 | grep -i kraken2
conda search -c conda-forge -c bioconda kraken2

conda activate kraken2
module avail 2>&1 | grep -i metaphlan


conda config --remove envs_dirs /shared/home/vashokan/.conda/envs



To activate this environment, use:

    micromamba activate /shared/ifbstor1/projects/mudis4ls_is4_benchmark/miniforge3

Or to execute a single command in this environment, use:

    micromamba run -p /shared/ifbstor1/projects/mudis4ls_is4_benchmark/miniforge3 mycommand
